{
  "name": "turfmapp-frontend",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "python3 -m http.server 3000 --directory public",
    "build": "echo 'Static files - no build needed'",
    "preview": "python3 -m http.server 3001 --directory public",
    "lint": "echo 'Static HTML - no linting configured'",
    "test": "echo 'Static HTML - no tests configured'"
  },
  "dependencies": {},
  "devDependencies": {}
}